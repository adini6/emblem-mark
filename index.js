const inquirer = require('inquirer');
const fs = require('fs');
const questions = require('./lib/questions.js');
const { Triangle, Circle, Square } = require('./lib/shape.js');


function generateLogo(svg, data){
    fs.generateLogo
}